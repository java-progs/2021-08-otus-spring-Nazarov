spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: library_test
      #port: 0
      #database: test

mongock:
  runner-type: "ApplicationRunner"
  change-logs-scan-package:
    - ru.otus.homework.changelogsTest

logging:
  level:
    root: ERROR